---
phase: 01-content-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - "About section states current transition between courtsite and a Malaysian digital bank role"
    - "Currently section shows software engineer (incoming) @ digital bank with no open-to line"
    - "TracePace status block shows launched, v1.0.0, and tracepace.app URL"
    - "Blog posts list shows 'i train in blocks, i build the same way' as newest entry"
    - "Emergency Reschedule Request appears in things shipped"
    - "'repo is private & invitational' note remains in TracePace section"
    - "Location, exploring interests, and training details in Currently section are unchanged"
  artifacts:
    - path: "README.md"
      provides: "Profile README with all 5 content updates applied"
      min_lines: 130
      contains:
        - "between roles, soon joining"
        - "software engineer (incoming) @ digital bank"
        - "status    : launched"
        - "version   : v1.0.0"
        - "tracepace.app"
        - "i train in blocks, i build the same way"
        - "emergency reschedule requests"
        - "repo is private & invitational"
  key_links:
    - from: "README.md :: about section"
      to: "context decisions D-01, D-02"
      pattern: "between roles, soon joining"
    - from: "README.md :: currently section"
      to: "context decisions D-03, D-04, D-05"
      pattern: "software engineer \\(incoming\\) @ digital bank"
    - from: "README.md :: tracepace code block"
      to: "context decisions D-06 through D-10"
      pattern: "\\[tracepace\\.app\\]\\(https://tracepace\\.app\\)"
    - from: "README.md :: writing section"
      to: "context decisions D-11, D-12, D-13"
      pattern: "i train in blocks"
    - from: "README.md :: things shipped"
      to: "context decisions D-14"
      pattern: "emergency reschedule requests"
---

<objective>
Update all 5 stale content sections of README.md to reflect current professional status, shipped work, and writing — no structural changes, no new sections.

Purpose: Eliminate all outdated references (courtsite, TracePace beta, old blog post) so README presents an authentic, current professional snapshot.
Output: Updated README.md with all 5 CONT-* requirements applied.
</objective>

<execution_context>
@/home/uzer/uz6r/uz6r/.planning/phases/01-content-refresh/01-CONTEXT.md
</execution_context>

<context>
@/home/uzer/uz6r/uz6r/README.md
@/home/uzer/uz6r/uz6r/.planning/REQUIREMENTS.md
@/home/uzer/uz6r/uz6r/.planning/PROJECT.md

<interfaces>
This plan modifies README.md — a flat Markdown file, no code interfaces, no imports. All changes are text replacements using the Edit tool's oldString/newString matching. The three tasks target non-overlapping sections and can be applied in any order.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: Update About and Currently sections — remove courtsite references, reflect digital bank transition (CONT-01, CONT-02)</name>
  <files>README.md</files>
  <read_first>
    README.md — read current state before editing
    .planning/phases/01-content-refresh/01-CONTEXT.md — for decision specifics (D-01 through D-05)
  </read_first>
  <action>
    Make two edits to README.md:

    Edit A — About section (per D-01, D-02):
    Replace the "also a full stack engineer at courtsite." line with the transition statement.
    Use exactly:
    oldString:
    ```
    also a full stack engineer at courtsite.
    ```
    newString:
    ```
    between roles, soon joining one of malaysia's leading digital banks as a full stack engineer.
    ```

    Edit B — Currently section (per D-03, D-04, D-05):
    Replace the entire currently code block. Change role, remove open-to line, preserve everything else.
    Use exactly:
    oldString:
    ```
    location  : petaling jaya, malaysia
    role      : software engineer, full stack @ courtsite
    exploring : systems design, distributed workflows, iot
    training  : marathons, lifting, working towards hyrox
    open to   : interesting problems
    ```
    newString:
    ```
    location  : petaling jaya, malaysia
    role      : software engineer (incoming) @ digital bank
    exploring : systems design, distributed workflows, iot
    training  : marathons, lifting, working towards hyrox
    ```

    IMPORTANT: Preserve existing colons/spacing alignment. Keep location, exploring, and training lines verbatim (D-05). Do NOT touch the "things i've shipped" section's references to courtsite — those are accurate context for shipped work and must remain.
  </action>
  <verify>
    <automated>
      grep -c "between roles, soon joining" README.md | grep -q 1 && \
      grep -q "software engineer (incoming) @ digital bank" README.md && \
      grep -q "petaling jaya, malaysia" README.md && \
      grep -q "marathons, lifting, working towards hyrox" README.md && \
      grep -c "open to" README.md | grep -q 0 && \
      grep -c "also a full stack engineer at courtsite" README.md | grep -q 0
    </automated>
  </verify>
  <acceptance_criteria>
    - "between roles, soon joining one of malaysia's leading digital banks" appears exactly once in README.md
    - "software engineer (incoming) @ digital bank" replaces "software engineer, full stack @ courtsite"
    - "open to" line is removed entirely (no "open to" substring in the file)
    - "also a full stack engineer at courtsite" is absent
    - Location, exploring, training lines remain unchanged (same text, same formatting)
    - Courtsite references in "things i've shipped" section are preserved (grep for "courtsite" still returns matches from shipped items)
  </acceptance_criteria>
  <done>
    README.md no longer references courtsite as current employer. About section describes transition. Currently section shows incoming digital bank role without open-to tagline.
  </done>
</task>

<task type="auto">
  <name>task 2: Update TracePace status block and Blog posts section (CONT-03, CONT-04)</name>
  <files>README.md</files>
  <read_first>
    README.md — read current state before editing (after Task 1 changes if run sequentially)
    .planning/phases/01-content-refresh/01-CONTEXT.md — for decision specifics (D-06 through D-13)
  </read_first>
  <action>
    Make two edits to README.md:

    Edit A — TracePace code block (per D-06, D-07, D-08, D-09, D-10):
    Replace the first 2 lines of the code block. Replace only "public beta incoming" and "v0.5.0-alpha", and add a "url" line between version and tech.
    Use exactly:
    oldString:
    ```
    status    : public beta incoming
    version   : v0.5.0-alpha
    tech      : golang · graphql · next.js · framer-motion
    ```
    newString:
    ```
    status    : launched
    version   : v1.0.0
    url       : [tracepace.app](https://tracepace.app)
    tech      : golang · graphql · next.js · framer-motion
    ```

    Do NOT touch the "repo is private & invitational" note (D-10). Do NOT change the "features" line.

    Edit B — Writing/blog posts section (per D-11, D-12, D-13):
    Remove the "playing the long game" post and add "i train in blocks, i build the same way" as the newest entry.
    Use exactly:
    oldString:
    ```
    - [i don't know enough yet to keep it simple](https://medium.com/@uz6r/i-dont-know-enough-yet-to-keep-it-simple-d9b650dd08a6) · jan 2026
    - [playing the long game in a sprint-obsessed industry](https://medium.com/@uz6r/playing-the-long-game-in-a-sprint-obsessed-industry-17e37bbf20b1) · nov 2025
    ```
    newString:
    ```
    - [i train in blocks, i build the same way](https://medium.com/@uz6r/i-train-in-blocks-i-build-the-same-way-e1c496e0863b) · 2026
    - [i don't know enough yet to keep it simple](https://medium.com/@uz6r/i-dont-know-enough-yet-to-keep-it-simple-d9b650dd08a6) · jan 2026
    ```

    Order is newest first (D-13): training article first, then "i don't know enough yet".
    Do NOT change the paragraph above the list ("i write on medium about engineering...").
  </action>
  <verify>
    <automated>
      grep -q "status    : launched" README.md && \
      grep -q "version   : v1.0.0" README.md && \
      grep -q "\\[tracepace\\.app\\](https://tracepace\\.app)" README.md && \
      grep -c "playing the long game" README.md | grep -q 0 && \
      grep -q "i train in blocks, i build the same way" README.md && \
      grep -q "repo is private & invitational" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - TracePace code block starts with status "launched", version "v1.0.0", has url line with clickable tracepace.app link
    - "public beta incoming" is absent from the file
    - "v0.5.0-alpha" is absent from the file
    - "playing the long game in a sprint-obsessed industry" is absent
    - "i train in blocks, i build the same way" appears as first blog post (before "i don't know enough yet")
    - "repo is private & invitational" note still present (preserved per D-10)
    - Features line ("rdp path simplification · elevation charts · weather integration") is unchanged
  </acceptance_criteria>
  <done>
    TracePace status block reflects launched product at v1.0.0 with tracepace.app URL. Blog posts show current article as newest entry, old post removed.
  </done>
</task>

<task type="auto">
  <name>task 3: Add Emergency Reschedule Request to things shipped (CONT-05)</name>
  <files>README.md</files>
  <read_first>
    README.md — read current state before editing (after Task 1 and Task 2 changes if run sequentially)
    .planning/phases/01-content-refresh/01-CONTEXT.md — for decision specifics (D-14)
  </read_first>
  <action>
    Add the Emergency Reschedule Request shipped item to the "things i've shipped" bullet list.

    Insert a new bullet after the last existing bullet (Raspberry Pi IoT). The last bullet is:
    ```
    - 🥧 **raspberry pi iot** — automated venue lighting based on booking schedules
    ```
    Append a new line after it with the Emergency Reschedule Request bullet.

    Use oldString/newString to add it (replace the closing blank-line-after-bullet with the new content):
    oldString:
    ```
    - 🥧 **raspberry pi iot** — automated venue lighting based on booking schedules
    ```
    newString:
    ```
    - 🥧 **raspberry pi iot** — automated venue lighting based on booking schedules
    - 📋 **emergency reschedule requests** — structured workflow when self-service is unavailable, with partner centre review and email notifications
    ```

    Per D-14: use the exact description text as specified. The emoji 📋 (clipboard) is chosen to represent the structured workflow/request nature. Match the bullet formatting of existing items (hyphen, emoji, bold title, spaced em-dash, description).
  </action>
  <verify>
    <automated>
      grep -q "emergency reschedule requests" README.md && \
      grep -q "partner centre review and email notifications" README.md && \
      grep -q "📋" README.md
    </automated>
  </verify>
  <acceptance_criteria>
    - "emergency reschedule requests" appears in the "things i've shipped" list
    - Description matches D-14 exactly: "structured workflow when self-service is unavailable, with partner centre review and email notifications"
    - Bullet formatting matches existing items (hyphen, emoji, bold title, spaced em-dash)
    - Existing shipped items (scaled platform, payment integrations, booking workflows, e-invoice, Turnstile, full stack, Raspberry Pi) remain unchanged
  </acceptance_criteria>
  <done>
    Emergency Reschedule Request appears in things shipped as a full bullet item matching existing formatting conventions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub → Visitor | Profile README rendered and served by GitHub to unauthenticated visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | README.md content | mitigate | Content is version-controlled via git; unauthorized modifications are traceable via commit history. Branch protection on main prevents direct pushes. |
| T-01-02 | Spoofing | Links in README (badges, URLs) | accept | All outbound URLs use HTTPS. If a linked service (shields.io, medium.com) is compromised, links could redirect users — this is accepted as an external dependency risk beyond the scope of this static file. |
</threat_model>

<verification>
## Overall Phase Verification

Run all acceptance criteria checks after all 3 tasks complete:

```bash
echo "=== CONT-01: About section ===" && \
grep -c "between roles, soon joining" README.md && \
echo "=== CONT-02: Currently section ===" && \
grep -q "software engineer (incoming) @ digital bank" README.md && echo "role ok" && \
grep -c "open to" README.md | grep -q 0 && echo "open-to removed" && \
echo "=== CONT-03: TracePace ===" && \
grep -q "status    : launched" README.md && echo "status ok" && \
grep -q "version   : v1.0.0" README.md && echo "version ok" && \
grep -q "\\[tracepace\\.app\\](https://tracepace\\.app)" README.md && echo "url ok" && \
grep -q "repo is private & invitational" README.md && echo "repo note preserved" && \
echo "=== CONT-04: Blog posts ===" && \
grep -q "i train in blocks, i build the same way" README.md && echo "new post added" && \
grep -c "playing the long game" README.md | grep -q 0 && echo "old post removed" && \
echo "=== CONT-05: Things shipped ===" && \
grep -q "emergency reschedule requests" README.md && echo "shipped item added" && \
echo "=== Integrity: existing content preserved ===" && \
grep -q "petaling jaya, malaysia" README.md && echo "location preserved" && \
grep -q "marathons, lifting, working towards hyrox" README.md && echo "training preserved" && \
grep -q "raspberry pi iot" README.md && echo "existing shipped items preserved" && \
grep -q "world marathon majors" README.md && echo "tracepace features preserved" && \
echo "ALL CHECKS PASSED"
```
</verification>

<success_criteria>
- About section states transition between courtsite and digital bank role (CONT-01) ✓
- Currently section shows "software engineer (incoming) @ digital bank" with no open-to line (CONT-02) ✓
- TracePace status block shows "launched", "v1.0.0", includes tracepace.app URL (CONT-03) ✓
- Writing section shows "i train in blocks, i build the same way" as newest post, "playing the long game" removed (CONT-04) ✓
- Emergency Reschedule Request appears in things shipped (CONT-05) ✓
- Stack/skills section remains unchanged ✓
- Location, exploring interests, training details unchanged ✓
- TracePace "repo is private & invitational" note unchanged ✓
- README retains all existing structure and formatting conventions ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-refresh/01-01-SUMMARY.md` documenting:
- Exact changes made to each section
- Any formatting decisions made at OpenCode's discretion (transition phrasing, emoji choice, bullet placement)
- Verification results (list of passed grep checks)
</output>
